import 'package:flutter/material.dart';

class ApplyFoundation extends StatelessWidget {
  const ApplyFoundation({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Under construction"),
      ),
    );
  }
}
